# AGENTS.md

## Project Germania Codex 工作规范

### 1. 项目名称

德国汽车市场价格与销量智能监测平台

英文名称：

Project Germania

---

## 2. 项目目标

本项目用于建立一套面向德国汽车市场的数据采集、清洗、存储、分析、预测和可视化系统。

系统需要长期监测德国重点车型的：

* 新车注册量；
* 官方指导价；
* 市场挂牌价；
* 价格变化；
* 在售库存；
* 地区差异；
* 车型版本；
* 动力类型；
* 汇率；
* 数据质量；
* 市场竞争关系。

最终形成：

1. 自动化数据采集系统；
2. 专属汽车市场数据库；
3. 价格和销量分析模块；
4. 时间序列预测模块；
5. Streamlit可视化平台；
6. 每日自动更新流程；
7. 可复现、可审计、可扩展的GitHub项目。

---

## 3. 业务背景

德国是欧洲最大的汽车市场之一，也是大众、宝马、奔驰、奥迪等品牌的核心市场。

本项目重点研究以下竞争关系：

* 德国传统燃油车型与新能源车型；
* 德国本土品牌与中国汽车品牌；
* 特斯拉与欧洲传统车企；
* 中国新能源SUV与德国主流SUV；
* 官方指导价与市场挂牌价；
* 价格变化与注册量变化；
* 不同平台和不同地区之间的价格差异。

本项目中的“销量”应优先使用德国联邦机动车管理局KBA发布的“新车注册量”。

不得将市场挂牌数量、网站在售数量或搜索结果数量描述为真实销量。

---

## 4. 初始重点研究车型

### 4.1 德国传统市场基准车型

* 大众高尔夫；
* 大众途观。

### 4.2 欧洲主流新能源车型

* 大众ID.3；
* 大众ID.4；
* 斯柯达恩雅；
* 宝马iX1；
* 奔驰EQA；
* 奥迪Q4 e-tron；
* 特斯拉Model Y。

### 4.3 中国品牌重点车型

* 比亚迪海豹U；
* 比亚迪元PLUS；
* 名爵4；
* 小鹏G6；
* 零跑C10；
* 蔚来EL6。

车型名单必须存放在配置文件中，不得分散写死在多个Python模块里。

建议配置路径：

`config/vehicles.yaml`

新增车型时应优先更新配置文件，而不是直接修改采集器业务逻辑。

---

## 5. 权威数据源

### 5.1 KBA

德国联邦机动车管理局。

主要用于：

* 月度新车注册量；
* 年度新车注册量；
* 品牌注册量；
* 车型系列注册量；
* 动力类型统计。

KBA数据必须保存：

* 原始文件；
* 文件下载地址；
* 数据统计周期；
* 下载时间；
* 原始字段名称；
* 字段映射记录。

### 5.2 ACEA

欧洲汽车制造商协会。

主要用于：

* 欧洲市场总体规模；
* 动力类型市场份额；
* 各国汽车市场比较；
* 欧洲新能源汽车趋势。

ACEA通常作为市场背景和宏观分析数据源，不应代替KBA车型级注册数据。

### 5.3 汽车厂商德国官网

主要用于：

* 官方基础价格；
* 官方起售价；
* 车型年款；
* 配置版本；
* 动力形式；
* 功率；
* 电池容量；
* 续航；
* 官方促销信息。

必须区分：

* 官方基础价格；
* 官方起售价；
* 促销价格；
* 补贴后价格；
* 贷款月供；
* 租赁月供。

租赁月供不得作为车辆官方售价保存。

### 5.4 AutoScout24和Mobile.de

主要用于：

* 市场挂牌价；
* 在售数量；
* 车辆地区；
* 首次注册年份；
* 里程；
* 卖家类型；
* 车辆状态；
* 价格历史；
* 库存变化。

挂牌价格不是实际成交价格。

不得将挂牌价格直接描述为“真实成交价”。

若项目需要估算成交价，必须使用单独字段和模型，并明确标记：

`estimated_transaction_price`

估算结果不得与真实来源数据混合。

### 5.5 汇率数据

优先使用欧洲中央银行或其他权威公开来源。

历史价格换算必须使用对应日期的历史汇率，不得统一使用当前汇率转换全部历史数据。

---

## 6. 数据合规原则

所有采集器必须遵守以下规则：

1. 优先使用公开API、CSV、Excel、JSON、ZIP或官方可下载文件；
2. 其次使用公开静态HTML；
3. 必要时才使用Playwright；
4. 不绕过验证码；
5. 不绕过账户登录；
6. 不破解访问控制；
7. 不开发Cloudflare绕过功能；
8. 不使用反检测技术隐藏自动化行为；
9. 不无限重试被拒绝的请求；
10. 网站拒绝访问时应停止并记录错误；
11. 设置合理访问间隔；
12. 设置每日请求总量限制；
13. 保存数据来源和采集时间；
14. 不采集与项目目标无关的个人数据；
15. 不在代码中伪造真实抓取结果。

若某网站不适合自动化采集，应提供以下替代方案之一：

* 官方下载文件导入器；
* 用户手动导出的CSV导入器；
* 合规第三方数据服务适配器；
* 暂停该数据源并记录原因。

---

## 7. 推荐技术栈

### 7.1 Python

使用：

* Python 3.12；
* pathlib；
* typing；
* dataclasses或Pydantic；
* logging；
* pytest；
* ruff；
* black；
* mypy或pyright。

### 7.2 数据采集

使用：

* httpx；
* requests，仅在已有模块中保留；
* Playwright；
* pandas；
* openpyxl；
* BeautifulSoup或selectolax。

除非存在明确理由，不要同时在同一模块混用多个HTTP客户端。

### 7.3 数据库

使用：

* SQLAlchemy 2.x；
* Alembic；
* SQLite用于本地开发；
* PostgreSQL用于正式部署。

数据库业务逻辑必须兼容SQLite和PostgreSQL。

禁止在核心业务模块中依赖SQLite专属语法。

### 7.4 分析与预测

使用：

* pandas；
* numpy；
* scipy；
* statsmodels；
* scikit-learn；
* Prophet作为可选模块；
* XGBoost或LightGBM仅在数据量足够时引入。

### 7.5 可视化

使用：

* Streamlit；
* Plotly；
* Matplotlib仅用于导出静态报告图片。

Dashboard不得使用写死的业务数字。

### 7.6 自动化与部署

使用：

* GitHub Actions；
* Docker；
* docker compose；
* PostgreSQL；
* 云服务器Cron或任务调度系统。

---

## 8. 项目目录规范

推荐目录：

```text
project-germania/
├── AGENTS.md
├── README.md
├── pyproject.toml
├── .env.example
├── .gitignore
├── config/
│   ├── vehicles.yaml
│   ├── sources.yaml
│   ├── analytics.yaml
│   └── settings.yaml
├── data/
│   ├── raw/
│   ├── interim/
│   ├── processed/
│   └── exports/
├── database/
├── docs/
├── logs/
├── notebooks/
├── scripts/
├── src/
│   └── germania/
│       ├── collectors/
│       ├── cleaning/
│       ├── database/
│       ├── analytics/
│       ├── forecasting/
│       ├── dashboard/
│       ├── quality/
│       └── utils/
└── tests/
    ├── fixtures/
    ├── unit/
    └── integration/
```

不得随意在项目根目录增加临时Python文件。

临时实验代码应放入：

`notebooks/`

或：

`scripts/experimental/`

稳定后再迁移到正式模块。

---

## 9. 文件职责

### config

保存：

* 车型名单；
* 数据源配置；
* 请求频率；
* 分析指标权重；
* 预测模型参数；
* 数据质量阈值。

### data/raw

保存未经修改的原始数据。

原始数据一旦保存，不得被覆盖或人工改写。

推荐路径：

```text
data/raw/{source_name}/{YYYY-MM-DD}/
```

### data/interim

保存：

* 初步解析结果；
* 未匹配车型；
* 待人工检查记录；
* 清洗中间文件。

### data/processed

保存：

* 已清洗数据；
* 数据质量报告；
* 平台对比结果；
* 可分析的数据集。

### data/exports

保存：

* CSV导出；
* Excel导出；
* 报告图表；
* 模型评估结果。

### src/germania/collectors

只负责：

* 数据获取；
* 原始响应保存；
* 基础解析。

不得在采集器中堆积全部分析和预测逻辑。

### src/germania/cleaning

负责：

* 字段标准化；
* 文本标准化；
* 车型名称映射；
* 金额标准化；
* 日期标准化；
* 里程标准化；
* 重复数据处理。

### src/germania/database

负责：

* SQLAlchemy模型；
* 数据库连接；
* Repository；
* 数据库迁移；
* 事务；
* 初始化和种子数据。

### src/germania/analytics

负责：

* 销量指标；
* 价格指标；
* 库存指标；
* 竞争指标；
* RVI研究价值指数。

### src/germania/forecasting

负责：

* 特征生成；
* 模型训练；
* 时间序列验证；
* 模型评估；
* 预测保存；
* 模型版本管理。

### src/germania/dashboard

负责：

* Streamlit页面；
* 数据查询；
* 图表组件；
* 用户筛选；
* 结果展示。

### src/germania/quality

负责：

* 数据完整性；
* 唯一性；
* 有效性；
* 一致性；
* 及时性；
* 数据问题记录。

---

## 10. 编码规范

所有新增Python代码必须遵守：

1. 使用Python 3.12语法；
2. 使用类型注解；
3. 公共函数编写docstring；
4. 优先使用pathlib；
5. 使用logging；
6. 不大量使用print；
7. 函数职责单一；
8. 避免超长函数；
9. 避免重复代码；
10. 避免全局可变状态；
11. 配置与代码分离；
12. 所有外部输入必须验证；
13. 网络请求必须有timeout；
14. 重试次数必须有限；
15. 数据库操作使用事务；
16. 错误不能被静默忽略；
17. 异常必须包含可定位的上下文；
18. 不使用裸except；
19. 不提交调试代码；
20. 不提交真实密码或密钥。

建议格式：

```python
from __future__ import annotations

from pathlib import Path
import logging

logger = logging.getLogger(__name__)
```

---

## 11. 命名规范

### Python文件和函数

使用：

`snake_case`

例如：

* `kba_collector.py`
* `normalize_vehicle_name()`
* `calculate_price_index()`

### 类名

使用：

`PascalCase`

例如：

* `KBACollector`
* `PriceObservation`
* `VehicleRepository`

### 数据库表

统一使用复数、小写、下划线：

* `vehicles`
* `price_observations`
* `sales_registrations`

### 数据库字段

统一使用英文和snake_case。

示例：

* `canonical_model`
* `official_price`
* `observed_at`
* `registration_count`

不得使用中文数据库字段名。

中文名称作为字段值保存。

---

## 12. 时间规范

1. 数据库时间戳统一保存为UTC；
2. 展示层可转换为Europe/Berlin；
3. 自动任务调度应明确UTC和柏林时间差异；
4. 日期和时间字段不得混淆；
5. 必须区分：

   * `collected_at`
   * `observed_at`
   * `valid_date`
   * `period_start`
   * `period_end`
   * `first_seen_at`
   * `last_seen_at`

不得使用单一`date`字段表达所有时间概念。

---

## 13. 金额和汇率规范

1. 金额必须使用Decimal或数据库Numeric；
2. 不使用float保存价格；
3. 保存原始货币；
4. 保存原始金额；
5. 保存换算后欧元价格；
6. 保存使用的汇率；
7. 保存汇率日期；
8. 历史数据必须使用历史汇率；
9. 缺少汇率时不得默认设为1；
10. 缺少汇率时应记录数据质量问题。

推荐字段：

* `original_price`
* `original_currency`
* `price_eur`
* `exchange_rate`
* `exchange_rate_date`

---

## 14. 核心数据库原则

数据库至少应支持以下实体：

* brands；
* vehicles；
* vehicle_variants；
* data_sources；
* regions；
* listings；
* price_observations；
* official_prices；
* sales_registrations；
* exchange_rates；
* collection_runs；
* data_quality_issues；
* forecast_results。

### 数据库要求

1. 所有表必须有主键；
2. 关联表必须有外键；
3. 高频查询字段建立索引；
4. 关键业务字段设置唯一约束；
5. 使用Alembic迁移；
6. 不直接手工修改正式数据库结构；
7. 数据迁移必须支持升级；
8. 如有必要，应支持回滚；
9. 数据库操作需要事务；
10. 任务失败时不得写入部分错误数据；
11. 每日任务必须幂等；
12. 同一数据重复运行不应产生重复记录。

---

## 15. 数据分层原则

项目采用三层数据结构。

### Raw Layer

保存原始数据。

特点：

* 不修改；
* 可追溯；
* 可重新解析；
* 包含来源URL；
* 包含采集时间；
* 包含原始哈希。

### Clean Layer

保存：

* 标准字段；
* 标准车型；
* 标准价格；
* 标准日期；
* 标准地区；
* 清洗状态；
* 匹配置信度。

### Analytics Layer

保存或动态计算：

* 平均价格；
* 中位数；
* 注册量；
* 市场份额；
* 库存；
* 价格变化；
* RVI；
* 预测结果。

禁止Dashboard直接大量解析原始HTML或原始JSON。

---

## 16. 增量更新规范

挂牌数据状态至少包含：

* NEW；
* UNCHANGED；
* PRICE_CHANGED；
* UPDATED；
* MISSING；
* INACTIVE；
* REACTIVATED。

规则：

1. 首次出现设为NEW；
2. 价格不变时不得重复插入价格历史；
3. 价格变化时新增价格观察记录；
4. 一次未抓到不能直接判断下架；
5. 采集失败时不得把全部记录设为inactive；
6. 只有采集成功且覆盖率达到阈值后才能判断缺失；
7. 连续多次未出现后再设为inactive；
8. 重新出现时设为reactivated；
9. 保存首次出现时间和最后出现时间；
10. 每次运行生成增量摘要。

---

## 17. 数据质量规范

至少计算：

* completeness_rate；
* uniqueness_rate；
* validity_rate；
* consistency_rate；
* timeliness_rate；
* overall_quality_score。

必须识别：

* 缺失值；
* 重复值；
* 无效日期；
* 负数价格；
* 异常里程；
* 错误车型匹配；
* 货币缺失；
* 汇率缺失；
* 官方价格类型混淆；
* 月度注册量异常；
* 同一挂牌同日价格冲突。

未知或无法确定的数据不得随意补全。

低置信度车型匹配应进入人工复核。

---

## 18. 车型匹配规范

车型标准化必须结合：

* 品牌；
* 车型名称；
* 别名；
* 动力类型；
* 年款；
* 版本；
* 页面上下文。

必须避免以下错误：

* 将大众ID.4匹配成ID.5；
* 将宝马X1匹配成宝马iX1；
* 将比亚迪海豹匹配成比亚迪海豹U；
* 将MG HS匹配成MG4；
* 将特斯拉Model 3匹配成Model Y。

每次匹配应尽量输出：

* 标准品牌；
* 标准车型；
* 匹配置信度；
* 匹配规则；
* 原始值。

低于阈值时不得自动写入正式车型ID。

---

## 19. 分析模型规范

分析指标必须基于数据库真实记录计算。

不得在Dashboard或分析模块中写死演示结果。

至少支持：

### 销量指标

* 月度注册量；
* 年度累计注册量；
* 同比增长率；
* 环比增长率；
* 品牌市场份额；
* 车型市场份额。

### 价格指标

* 平均挂牌价；
* 价格中位数；
* 价格标准差；
* 价格变异系数；
* 价格分位数；
* 平均降价幅度；
* 降价挂牌比例；
* 官方价与挂牌价差。

### 库存指标

* 活跃挂牌量；
* 新增挂牌量；
* 下架代理指标；
* 平均在售时间；
* 地区库存分布。

### 竞争指标

* 中国品牌与欧洲品牌价格差；
* 同级车型价格指数；
* 价格与注册量相关性；
* 车型研究价值指数RVI。

---

## 20. RVI车型研究价值指数

建议默认公式：

```text
RVI =
0.30 × 市场规模得分
+ 0.20 × 增长动能得分
+ 0.15 × 价格波动得分
+ 0.15 × 库存活跃得分
+ 0.10 × 战略相关性得分
+ 0.10 × 数据质量得分
```

要求：

1. 权重保存在配置文件；
2. 所有分项标准化到0—100；
3. 输出原始值；
4. 输出标准化值；
5. 输出最终得分；
6. 数据不足时返回`insufficient_data`；
7. 不得随意生成排名；
8. 战略相关性必须有公开规则；
9. 最终结果应注明这是项目模型，不是官方排名。

---

## 21. 预测模型规范

任何预测模型都必须与基线比较。

### 基线模型

* 上一期值；
* 移动平均；
* 季节性朴素模型。

### 统计模型

* 指数平滑；
* SARIMA；
* Prophet可选。

### 机器学习模型

只有样本量充足时才能使用：

* XGBoost；
* LightGBM。

### 预测要求

1. 时间序列按时间划分；
2. 禁止随机打乱时间序列；
3. 禁止未来数据泄漏；
4. 使用walk-forward validation；
5. 保存训练时间范围；
6. 保存模型版本；
7. 保存预测区间；
8. 保存MAE、RMSE和sMAPE；
9. 数据不足时不得训练复杂模型；
10. 未优于基线的模型不得描述为有效提升；
11. 不得输出虚假准确率；
12. 必须固定随机种子；
13. 预测结果必须明确标记为模型输出。

---

## 22. Dashboard规范

Streamlit平台建议包含：

1. 市场总览；
2. 车型对比；
3. 价格监控；
4. 注册量分析；
5. 预测与预警；
6. 数据质量；
7. 自动任务状态。

Dashboard必须：

* 使用中文；
* 优先显示中文车型名称；
* 显示来源；
* 显示更新时间；
* 区分官方数据、挂牌数据和预测数据；
* 支持筛选；
* 数据不足时明确提示；
* 不显示写死的业务结果；
* 不泄露数据库地址和密码；
* 数据库异常时显示友好错误；
* 支持CSV导出。

---

## 23. 测试规范

所有新功能必须包含测试。

至少覆盖：

* 正常输入；
* 空值；
* 无效输入；
* 重复输入；
* 极端数值；
* 网络错误；
* 页面结构变化；
* 数据库事务失败；
* 汇率缺失；
* 车型误匹配；
* 同一任务重复运行。

### 网络测试

测试不得依赖实时网站。

使用：

* fixture；
* 保存的HTML；
* 保存的JSON；
* mock HTTP响应；
* 临时SQLite数据库。

### 测试命令

```bash
pytest
ruff check .
black --check .
```

如果项目配置了类型检查，还需要运行：

```bash
mypy src
```

不得声称未执行的测试已经通过。

---

## 24. 环境变量规范

所有秘密信息必须通过环境变量传入。

例如：

```text
DATABASE_URL=
LOG_LEVEL=
RAW_DATA_DIR=
PLAYWRIGHT_HEADLESS=
REQUEST_TIMEOUT=
MAX_REQUESTS_PER_RUN=
```

要求：

* 提供`.env.example`；
* 不提交`.env`；
* 不在README中写真实密码；
* 不在日志中输出完整数据库URL；
* 不在异常消息中泄露密钥；
* GitHub Actions使用GitHub Secrets。

---

## 25. Git规范

Codex修改代码时应：

1. 先阅读现有文件；
2. 查看当前git状态；
3. 不覆盖用户未提交的修改；
4. 不删除不相关功能；
5. 修改范围保持聚焦；
6. 更新测试；
7. 更新README或对应文档；
8. 报告修改文件；
9. 报告测试结果；
10. 报告仍存在的问题。

不应在一次任务中进行与目标无关的大规模重构。

除非用户明确要求，否则不要：

* 修改仓库名称；
* 删除历史数据；
* 重写全部架构；
* 替换主要技术栈；
* 强制推送；
* 删除分支；
* 自动发布生产环境。

---

## 26. Codex执行任务前的步骤

每次收到任务后，必须先：

1. 阅读`AGENTS.md`；
2. 阅读`README.md`；
3. 查看项目目录；
4. 查看`git status`；
5. 定位现有相关代码；
6. 明确任务范围；
7. 说明准备修改哪些文件；
8. 检查是否存在未完成的前置模块。

如果当前仓库结构与任务描述不一致，应先报告差异，不要盲目新建重复模块。

---

## 27. Codex执行任务中的要求

开发过程中必须：

1. 优先复用已有模块；
2. 保持向后兼容；
3. 小步修改；
4. 新增测试；
5. 使用真实接口结构或本地fixture；
6. 不伪造真实运行成功；
7. 网络不可用时明确说明；
8. 不隐瞒失败；
9. 不静默删除异常记录；
10. 不跳过数据库迁移；
11. 不将临时代码留在生产模块；
12. 不修改与任务无关的文件。

---

## 28. Codex完成任务后的固定输出

每次任务完成后必须输出：

### 已完成

列出已完成的功能。

### 修改文件

列出：

* 新增文件；
* 修改文件；
* 删除文件。

### 运行命令

列出用户需要执行的命令。

### 测试结果

包括：

* pytest；
* ruff；
* black；
* 类型检查；
* 数据库迁移；
* dry-run。

### 数据结果

如适用，报告：

* 原始记录数；
* 有效记录数；
* 去重记录数；
* 插入数量；
* 更新数量；
* 拒绝数量；
* 数据质量评分。

### 风险

说明：

* 网站结构风险；
* 数据缺失；
* 匹配问题；
* 合规问题；
* 部署限制。

### 未完成

明确列出尚未完成内容。

### 下一步建议

只给出下一阶段建议，不得自动开始下一阶段。

---

## 29. 禁止事项

Codex不得：

* 伪造KBA数据；
* 伪造网页抓取成功；
* 伪造测试通过；
* 绕过验证码；
* 绕过登录；
* 破解网站；
* 隐藏自动化行为；
* 将挂牌价描述成成交价；
* 将新车注册量错误描述成企业真实销售收入；
* 将预测值伪装成真实值；
* 将今天汇率用于全部历史价格；
* 删除原始数据；
* 覆盖历史价格；
* 将数据库密码写入代码；
* 将车型名称写死在多个模块；
* 对无法匹配车型进行猜测；
* 自动提交大文件；
* 自动提交数据库文件；
* 自动部署到生产环境；
* 在未获许可时大规模高频访问网站。

---

## 30. 当前项目实施顺序

项目应按以下顺序开发：

1. 项目骨架；
2. 车型配置和数据字典；
3. 数据库ER设计；
4. SQLAlchemy和Alembic；
5. 统一采集器接口；
6. 汇率数据；
7. KBA注册量；
8. 单一汽车厂商官网；
9. 单一车型AutoScout24采集；
10. 数据清洗；
11. 增量更新；
12. 扩展车型；
13. 第二挂牌平台；
14. 分析指标；
15. 预测模型；
16. Streamlit Dashboard；
17. GitHub Actions；
18. PostgreSQL和Docker；
19. 最终审计。

不得在数据库和清洗逻辑尚未稳定前，直接同时开发全部车型采集器。

优先使用一辆车型跑通完整链路，再逐步扩展。

推荐第一辆车型：

大众高尔夫。

---

## 31. 最终原则

本项目的最高优先级依次为：

1. 数据真实性；
2. 数据可追溯性；
3. 数据合规性；
4. 数据质量；
5. 系统稳定性；
6. 可解释分析；
7. 自动化；
8. 预测复杂度；
9. 界面美观。

不得为了展示效果牺牲数据真实性。

不得为了模型复杂度忽略样本量。

不得为了自动化而绕过数据源限制。

项目的最终价值不是“抓取了多少条数据”，而是建立一套真实、稳定、可持续更新、能够支持企业决策的德国汽车市场数据系统。
